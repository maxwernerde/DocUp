spring:
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:15432/clinic_data
    username: clinic_user
    password: pAssw0rd

  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate.jdbc.lob.non_contextual_creation: true
